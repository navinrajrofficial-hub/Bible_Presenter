vasanum title in the top

when add songs to main slide auto save in the log. -> done

Remote Control + Mobile Viewer: a phone controller for next/prev, black screen, jump‑to, and a separate low‑latency “viewer” mode for TVs or overflow rooms.

one toggle to disable remote control

make phone ui beautiful

implement almost all the navigation features, without the adding stuff.

in the song ddl for history, if any history is there, that should prepopulate first in the ddl. we just need to click load.

add bookmarks in the psrn file as well.
currently only slides are being saved.

